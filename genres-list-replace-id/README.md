<span style="font-size: 0.8em; color: grey;">
Gogi Doe - 8 Dec 2025
</span>

# *чому масиви даних так важко шатать?*

дароуті!

недавно в нашій [форумній спільноті](https://t.me/bui_studies) запостив [задачку](https://t.me/bui_studies/8/3343). здалось шо це прям прикольна? не складна, дуже практична штука, з якою може впоратись ну майже будь хто.

а виявилось - пара людей подивились на неї, подумали і забили болта рабить, бо шось якось воно не туйво.

тому, власне, нате-тримайте рішення з поясненнями і коментарями.

---

### *структура даних*

масив, блядь! прикинь?

а якшо серйозно, то це список всіх-всіх жанрів музики які тіки є на сайті [epidemic sound](https://www.epidemicsound.com/).

дані чесно спіжжено зі [сторінки](https://www.epidemicsound.com/music/genres/) зі списком всіх жанрів із списку запитів які робляться на бекенд. там прилітають і жанри і настрої. але нас настрої ніхуяшеньки не цікавлять якшо чесно, тому лишаєм тіки жанри.

кожен обʼєкт вигляда приблизно отак:

```json
{ 
  "id": "01242e3c-61a0-4981-96d1-3a7fde9e20b2",
  "fatherGenreId": "aefcdfa3-aee7-45c5-9482-813ea1226c02",
  "tag": "Funerals",
  "displayTag": "Funerals",
  "slug": "funerals",
  "numTracks": 11
}
```

з чого робицця висновок шо у жанрів є над-жанри, батьківські жанри. типу як "Metall" та "Death Metall", "Punk" і "Pop-punk", тощо.

---

### *шо нада було зрабить?*

а все дюже просто. о прям цитую пост зі спільноти:

> перегенерувати ВСІ _id_ та _fatherGenreId_.

ба більше. я подумав ну може таке бути людям буде важко зклеїти 2 та 2 шоби отримать 4, тому описав послідовність дій які тре виконувати:

> для кожного наступного обʼєкта в масиві:  
> 1 - взяти його id  
> 2 - зібрати обʼєкти, в яких згадується цей id в полі fatherGenreId  
> 3 - згенерувати новий id  
> 4 - замінити старий на новий і в полі id поточного обʼєкита і в полі fatherGenreId всіх де є згадка.

шо тут іще пояснювати? а?!

от і я не їбу. єдине уточнення до якого моя башка додумалась - це сказати шо в пункті 2 мона використовувати таку струкутуру даних як Map в жабасквірті. потрясна хуйовіна, там і ключі і їх значення можуть бути чим завгодно. в нашому випадку на один обʼєкт можна лінкать всі обʼєкти де є згадка в _fatherGenreId_.

---

### як я це думав зрабить?

а дюже просто!

задум був такий шоб в `Map` запиховувати ключами - обʼєкти жанрів, а значенями цих ключів - масив дочірніх жанрів.

пресдоков приблизно такий:
```javascript
const changeGenreIds = src => {
  const fatherChildrenGenresMap = src.reduce((acc, currGenre) => {
    // магічним чином шукаю батьківський жанр
    const parentGenre = getParentGenreById(currGenre.fatherGenreId)
    // по батьківському жанру або тінемо або створюжємо Set сестринських жанрів, шоби не їбать голову з повтореннями. 
    const siblings = acc.getOrInsert(parentGenre, new Set())
    // Set має все почистити за нас, якшо ми додали якийсь жарн вдруге
    siblings.add(currGenre)

    // оновлення айдішок що повʼязані з батьківським жанром
    const newParentId = generateNewId()
    parentGenre.id = newParentId
    siblings.forEach(s => s.fatherGenreId = newParentId)

    // оновлення айдішок, що повʼязані з поточним жанром
    const newId = generateNewId()
    currGenre.id = newId
    const childGenres = acc.getOrInsert(currGenre, new Set())
    childGenres.forEach(g => g.fatherGenreId = newId)
  }, new Map())

  return serializeMap(fatherChildrenGenresMap)
}
```
`Big O` такої хєробори - **квадратне**😰!

тобто час на виконання операції ростиме, як кількість зборів на ЗСУ і їх суми за останні пару років - експоненційно!

я вже мовчу про те шо міфічний `getParentGenreById` бознашо в собі має містити шоби не потягти цей корабель на дно. 

не існує в `Map` методу, який би по ключах шукав колбеком типу `someMap.get(key => key.id === 'zalupa)`. 

Нажаль, боженька не видумав такого в природу.

---

### і шо тепер?

а нічо. поки я собі носив цю ідею в голові, нам уже усьо підписник [засабмітив](https://github.com/YaroslavUdych/regenIds/blob/main/index.js)🫠. його варіант в моє бачення не до кінця залазить, тому будем ліпить *франкенштейна*!

підхід, використаний підписником передбачає що все що нам насправді потрібно - мати десь записаний звʼязок між старою `id`-шкою та новою.

фсьо.

далі, ти по суті просто один раз проходишся по масиву даних вдруге, і міняєш грьобані айдішки згідно новим референсам, які у тебе уже лежать поруч!

геніально, блін! дійсно геніально! так, два проходи одним і тим же здоровезним масивом, але давайте хочаб з таким прикладом розберемось, потім будем крокувати далі.

отож:
```javascript
// функція яка вміє рабить обʼєкт { [oldId]: newId }
const getIdsMap = items => items.reduce((acc, item) => {
  acc[item.id] = `NEW__${uuidv4()}`; //буквально ЄДИНЕ що вона робить
  return acc;
}, {})

const refreshIds = (items, idsMap) => items.map(item => {
  const {id, fatherGenreId, ...restProps} = item
  // захист від дібіла
  if (id === fatherGenreId) throw new Error('Ideantifier must be different from fatherGenreId.');

  // знаходимо відповідники старим айдішкам 
  const newId = idsMap[id];
  const newFatherGenreId = fatherGenreId ? idsMap[fatherGenreId] : null;

  // захист від ще одного дібіла
  if (newId === id && newId === fatherGenreId) throw new Error('Ideantifier must be different from fatherGenreId after change.');

  // випльовуємо на волю усе це разом
  return {
    id: newId,
    parentGenreId: newFatherGenreId,
    ...restProps
  }
})
```

от на такому розвʼязанні я вирішив зупинитись, бо й без того вшатав добряче часу і на роздуми, і на документацію, і на статтю. 

---

### шо можна покращити,

та по суті ніхіба.

я переговорювався із собою, з чатом джіпіті, і ми дійшли висновку шо як ти не вкакайся, а роботи буде робитись пропорційно до загальної кількості жанрів.

навіть якщо спробувати якось запустити 2 проміси, один з яких лупаситиме все шо нада на першу половину масиву, а інжий - піде з кінця, то...

ну скажімо так, теоретично можна заритись носом у воркери щоб змусити це дійсно процювати. але навіть із воркерами загальна складність буде O(n), як мені пояснив чатік.

так шо усьо, фініта ля комадія, виходить!

далі буде...